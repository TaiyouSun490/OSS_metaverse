# Contributing

Issues and pull requests are welcome. Keep the core provider-neutral, place optional SDK code in a define-constrained integration assembly, and add tests for protocol changes. Public protocol changes must remain backward-compatible or increment the protocol name/version.

Run the Unity Test Framework EditMode tests before submitting a change. Do not commit service keys, upload tokens, generated bundles, or user-generated content.
